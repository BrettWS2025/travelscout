{
  "name": "travelscout-monorepo",
  "private": true,
  "description": "TravelScout monorepo \u2014 the Next.js app lives in web/",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "vercel-build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web",
    "test": "npm run test --prefix web"
  },
  "engines": {
    "node": "22.x"
  }
}
