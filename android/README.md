# Android

Placeholder for the TravelScout Android app.

Shared backend: Supabase project + migrations in `/supabase`. Prefer a shared API contract with `web/` rather than importing Next.js code directly.
